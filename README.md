### Задание 2 по дисциплине "Аналитика данных" - API-пайплайн

<b>Задача:</b> данный скрипт должен читать отзывы из входного файла (параметры ниже) и отправлять их в LLM для определения тональности и тематики (описание ниже). Ответ LLM должен обрабатываться и записываться в CSV.
<i>[Важно] Скрипт работает с LLM с помощью API.</i>

Категории тональности, которые определяет скрипт:

- positive
- negative
- neutral

Категории тематики отзывов, которые определяет скрипт:

- <i> потом дополнить ... </i>

Требования к входному файлу:

- Файл должен быть в формате CSV.
- Должен иметь следующую структуру:

<i>[Пример входного файла]</i>
